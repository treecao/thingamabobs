<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TBD - SOMETHINSOMETHIN</title>
  {{!-- bulma CSS for now, we can use a different framework later if y'all don't like it --}}
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
  <script defer src="https://use.fontawesome.com/releases/v5.3.1/js/all.js"></script>
</head>

  <div class="wrapper">
    <header>
        {{!-- TODO: DEFINE THE PAGE NAME --}}
        <h1 class="has-text-black px-3 py-3 has-text-weight-bold">MAIN FIRST PAGE HEADER</h1>
        {{!-- navigation bar --}}
        <nav class="navbar mt-2">
            {{#if loggedIn}}
            {{!-- navigate to home page --}}
            <a href="/" class="navbar-item is-hoverable pt-1">HOME</a>
            <a href="/{{!--TODO: add route to contact us here --}}" class="navbar-item is-hoverable pt-1">CONTACT US</a>
            <a href="/{{!--TODO: add route to Post Item for sale here --}}" class="navbar-item is-hoverable pt-1 mr-4">SELL</a>
            <a href="/{{!--TODO: add route to Shopping Cart here --}}" class="navbar-item is-hoverable pt-1 mr-4">BUY {{!--possibly add shopping cart icon here--}}</a>
            <button id="logout" class="button is-danger">logout</button>
            {{else}}
            <a href="/" class="navbar-item is-hoverable pt-1">HOME</a>
            <a href="/{{!--TODO: add route to contact us here --}}" class="navbar-item is-hoverable pt-1">CONTACT US</a>
            <a href="/{{!--TODO: add route to Shopping Cart as a guest here --}}" class="navbar-item is-hoverable pt-1 mr-4">TBD Shopping Cart Icon</a>
            <a href="/{{!!--TODO: add route To Log In here --}}" class="button is-primary">LOGIN</a>
            {{/if}}
        </nav>
    </header>

    <main>
            {{{ body }}}
    </main>

    <footer class="footer rows pb-2">
    {{!-- team members: Amrita, Daniel, Isaiah, Matt, Roman, Tree --}}
      <div class="row-is-full content has-text-centered">
    <p>
      <strong>Site Creators</strong> [heart emoji here] <a href="{{!--add gh--}}">Amrita</a> | <a href="{{!--add gh--}}">Daniel</a> | <a href="{{!--add gh--}}">Isaiah</a> | <a href="{{!--add gh--}}">Matt</a> | <a href="{{!--add gh--}}">Roman</a> | <a href="{{!--add gh--}}">Tree</a>
    </p>
  </div>
    </footer>
  </div>

{{#if loggedIn}}
<script src="/javascript/logout.js"></script>
{{/if}}
</body>

</html>